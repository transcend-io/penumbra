import { PenumbraDecryptionInfo } from '../types';
/**
 * An event emitter for the decryption progress
 * @param totalBytesRead the number of bytes read so far
 * @param contentLength the total number of bytes to read
 * @param url the URL being read from
 * @returns
 */
export default function emitEncryptionCompletion(id: number, decryptionInfo: PenumbraDecryptionInfo): void;
//# sourceMappingURL=emitEncryptionCompletion.d.ts.map